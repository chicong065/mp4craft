{
  "name": "mp4craft-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter mp4craft build",
    "dev": "pnpm --filter @mp4craft/playground dev",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "lint": "oxlint",
    "release": "pnpm release:check && pnpm --filter mp4craft publish --access public",
    "release:check": "pnpm typecheck && pnpm lint && pnpm format:check && pnpm test",
    "release:dry": "pnpm release:check && pnpm --filter mp4craft publish --access public --dry-run --no-git-checks",
    "test": "pnpm --filter mp4craft test",
    "typecheck": "tsc -b --noEmit"
  },
  "devDependencies": {
    "oxfmt": "^0.45.0",
    "oxlint": "^1.60.0",
    "oxlint-tsgolint": "^0.21.1",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.0"
}
